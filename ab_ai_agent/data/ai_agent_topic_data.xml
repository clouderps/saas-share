<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="topic_general_conversation" model="ai.agent.topic">
            <field name="name">General Conversation</field>
            <field name="code">general</field>
            <field name="description">Friendly back-and-forth, greetings, small clarifications. No tools.</field>
            <field name="instructions">For greetings and casual exchanges, respond naturally and briefly. Stay on topic. If the user asks for data, switch to the appropriate topic.</field>
        </record>

        <record id="topic_date_math" model="ai.agent.topic">
            <field name="name">Date &amp; Time Math</field>
            <field name="code">date_math</field>
            <field name="description">All date math goes through the date_reference tool. Never compute relative dates manually.</field>
            <field name="instructions">When the user references a relative period ("today", "this month", "last quarter", "next week"), call the `date_reference` tool first to get the exact date ranges; then use those dates in any downstream query.</field>
            <field name="tool_ids" eval="[(6, 0, [ref('tool_date_reference')])]"/>
        </record>

        <!-- Navigation — UI-level "open this" actions. -->
        <record id="topic_navigation" model="ai.agent.topic">
            <field name="name">Navigation</field>
            <field name="code">navigation</field>
            <field name="description">Open records, lists, pivots, graphs, reports — let the user navigate the app by voice or chat.</field>
            <field name="instructions">When the user wants to **see** something — not just a number, but the actual view in Odoo — call one of:
- `open_action` for built-in reports: P&amp;L (xmlid `account.action_account_pl_report`), Balance Sheet (`account.action_account_balance_report`), or any module action.
- `open_record` to land on a specific record's form view ("open SO 42", "show invoice INV/2026/00045").
- `open_list` for filtered list views ("show overdue invoices" → model=account.move with the matching domain).
- `open_pivot` for cross-tab analysis ("pivot sales by product and month").
- `open_graph` for charts ("pie chart of customers by country", "bar chart of sales by branch").

When the user names a document by its number and asks to confirm / post / validate it ("INV/2026/00001 please post", "SO00047 confirm", "WH/IN/00012 validate", "post payment PBNK1/2026/0003"), call `record_action` with the exact reference. It picks the right finalize per type (confirm SO/PO, post invoice/bill/payment, validate picking) and is safe — if it's already finalized or the agent lacks write permission you'll be told.

Always end with `__end_message` summarising what you opened/did (e.g. "Opening the P&amp;L for this month — the report should appear shortly." / "Invoice INV/2026/00001 posted.").</field>
            <field name="tool_ids" eval="[(6, 0, [
                ref('tool_open_record'),
                ref('tool_open_list'),
                ref('tool_open_pivot'),
                ref('tool_open_graph'),
                ref('tool_open_action'),
                ref('tool_record_action'),
            ])]"/>
        </record>

        <!-- System guidance — the "help me use Ghaima" half of the
             assistant. Every tool here answers from the CURRENT user's
             own access, so the instructions push hard on never
             inventing capability the user does not have. -->
        <record id="topic_system_guidance" model="ai.agent.topic">
            <field name="name">System Guidance</field>
            <field name="code">system_guidance</field>
            <field name="description">Explain what Ghaima does, what this user can reach, where to perform a task, and what a screen means — grounded in their real permissions.</field>
            <field name="auto_attach" eval="True"/>
            <field name="instructions">You also teach people how to use Ghaima. These questions are as important as the numbers.

**Never guess where something lives.** Odoo menu layouts differ per install and per user's access. Always call a tool first:

- `list_my_apps` — when the user asks what they can do, what they have access to, or where to begin ("what can I do here?", "ما الذي يمكنني فعله؟", "I'm new"). Answer ONLY from what it returns. If an app is not in the result the user cannot open it, so do not mention it.
- `find_menu` — for every how-do-I / where-is question ("where do I create a credit note?", "وين أسوي مرتجع؟", "how do I add an employee?"). Give the returned `path` verbatim, then call `open_action` with the match's `action_xmlid` so the answer carries a working button.
- `explain_screen` — when they ask what a screen, document type, field or status means. Use the real `key_fields` and `lifecycle` it returns rather than describing Odoo in general.

**Permissions are part of the answer, not a footnote.** `explain_screen` reports what THIS user may do. When `write` is false, do not tell them to change anything — say who can and stop. When `find_menu` returns no matches, say plainly that they do not have access to it and suggest asking their administrator. Never soften this into a vague "you may need permissions" — be specific about what you checked.

**Teach in the order they will act.** Lead with the destination (the menu path), then what they will see there, then the first thing to fill in. Two or three steps, not a manual. If the task genuinely needs more, offer to walk through it rather than dumping every step.

**Never expose the plumbing.** Do not name a tool, a model, or a field's technical name in your reply, and never ask the user to supply one — they are business users, not developers. If you cannot tell which screen they mean, ask in their words ("do you mean the customer invoices list, or the vendor bills?").

Always end with `__end_message`.</field>
            <field name="tool_ids" eval="[(6, 0, [
                ref('tool_list_my_apps'),
                ref('tool_find_menu'),
                ref('tool_explain_screen'),
                ref('tool_open_action'),
            ])]"/>
        </record>

        <!-- HR — attendance + leave roll-call. Gated to PII-allowed agents. -->
        <record id="topic_hr_roll_call" model="ai.agent.topic">
            <field name="name">HR Roll Call</field>
            <field name="code">hr_roll_call</field>
            <field name="description">Who's at work, who's on leave, who's still clocked in.</field>
            <field name="instructions">For HR-attendance questions ("who didn't check in today", "show pending leave requests", "who's still at the office"), call the matching tool. Render results as a data_table with columns Name / Department / Job (or Name / Type / Dates for leaves). Do NOT call these tools if the agent isn't PII-allowed.</field>
            <field name="tool_ids" eval="[(6, 0, [
                ref('tool_hr_attendance_missing_today'),
                ref('tool_hr_leave_pending'),
                ref('tool_hr_attendance_open_shifts'),
            ])]"/>
        </record>

        <!-- Analytics — accurate timeseries + charts via the
             data_analysis tool. The numbers never pass through the
             model; the LLM only writes the interpreting paragraph. -->
        <record id="topic_analytics" model="ai.agent.topic">
            <field name="name">Analytics &amp; Charts</field>
            <field name="code">analytics</field>
            <field name="description">Trends, comparisons and breakdowns of sales rendered as charts + KPIs.</field>
            <field name="instructions">For ANY question about how a metric is doing over time — "sales trend", "POS sales last 30 days", "compare this month to last", "weekly revenue", "how are we doing" — call the `data_analysis` tool. Pick metric (pos_sales / sale_orders / invoiced_revenue), period_days and group (day/week/month) from the question; call `date_reference` first only if the user gave a relative range you must convert. The tool returns the chart, KPI grid and peak callout already — do NOT repeat those numbers in prose. Your final answer is ONE short paragraph: what changed, why it matters, and one concrete recommended action.

For "latest / most recent / last N records" questions ("latest invoice added", "last 5 sale orders", "newest customers", "recent POS orders") call the `recent_records` tool with the matching kind — NEVER answer these from earlier conversation context or prior numbers. It returns the table; add one short sentence about the newest item. For a COMPOUND request ("latest SO and invoice and purchase", "recent customers and sales") call `recent_records` once per kind in separate steps — every result is automatically combined into one answer, so do not skip any kind the user asked for.</field>
            <field name="tool_ids" eval="[(6, 0, [
                ref('tool_data_analysis'),
                ref('tool_recent_records'),
                ref('tool_date_reference'),
            ])]"/>
        </record>

    </data>
</odoo>
